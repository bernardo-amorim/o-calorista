{
  "name": "o-calorista",
  "version": "1.0.0",
  "private": true,
  "description": "A WhatsApp-based calorie counting assistant",
  "workspaces": [
    "server",
    "frontend",
    "infrastructure"
  ],
  "scripts": {
    "server:dev": "cd server && bun run dev",
    "server:start": "cd server && bun run start",
    "server:test": "cd server && bun test"
  }
}
